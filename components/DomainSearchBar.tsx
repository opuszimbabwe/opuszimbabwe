'use client';import {Search} from 'lucide-react';import {useState} from 'react';export default function DomainSearchBar(){const[q,setQ]=useState('');const[done,setDone]=useState(false);return <div className="max-w-2xl"><form onSubmit={e=>{e.preventDefault();setDone(true)}} className="bg-white/90 rounded-full overflow-hidden shadow-xl flex items-center p-1.5"><Search className="ml-4 text-muted-fg" size={20}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search your domain" className="font-semibold placeholder:font-semibold flex-1 bg-transparent outline-none px-3 py-3"/><button className="bg-accent text-white rounded-full px-5 py-3 text-sm font-semibold">Search Domain</button></form>{done&&<div className="bg-white rounded-2xl p-4 mt-3 text-dark text-sm">{/* MOCK_BACKEND: replace with live Cloudflare availability check */}<b>{q||'yourdomain'}</b> is available in this demo.</div>}</div>}