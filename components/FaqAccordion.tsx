'use client';import {Plus} from 'lucide-react';import {useState} from 'react';export default function FaqAccordion({items}:{items:{q:string;a:string}[]}){const[open,setOpen]=useState<number|null>(null);return <div className="space-y-3">{items.map((x,i)=><div key={x.q} className={`rounded-2xl overflow-hidden ${open===i?'bg-white border border-border shadow-[0_10px_28px_rgba(23,19,31,0.08)]':'bg-muted'}`}><button onClick={()=>setOpen(open===i?null:i)} className="w-full flex justify-between items-center gap-4 px-6 py-4 font-semibold text-sm text-dark text-left">{x.q}<Plus className={`text-primary transition-transform ${open===i?'rotate-45':''}`}/></button><div className={`px-6 text-muted-fg text-sm leading-[1.6] transition-all duration-300 ${open===i?'max-h-96 pb-5':'max-h-0 overflow-hidden'}`}>{x.a}</div></div>)}</div>}